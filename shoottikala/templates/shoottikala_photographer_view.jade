extends base.jade
- load shoottikala_tags
- load crispy_forms_tags
block title
  | Ilmoittaudu valokuvaajaksi
block content
  if can_edit
    if photographer.instance.pk
      h2 {{ event.name }}: Muokkaa valokuvaajailmoitustasi
    else
      h2 {{ event.name }}: Ilmoittaudu valokuvaajaksi
  else
    h2 {{ event.name }}: Valokuvaajailmoitus

  if can_edit
    .panel.panel-default
      .panel-heading: strong Omat tietosi
      .panel-body
        - readonly user_form
      .panel-footer.clearfix
        .btn-group.pull-right
          a.btn.btn-default(href='{{ settings.KOMPASSI_HOST}}/profile', target='_blank') Kompassin käyttäjäprofiili…
        p.text-muted Mikäli tiedoissasi on korjattavaa, käy korjaamassa tietosi Kompassin käyttäjäprofiiliisi ja kirjaudu sen jälkeen uudelleen sisään.
        p.text-muted Nämä tiedot tulevat ainoastaan tapahtuman järjestäjien tietoon, ellet itse päätä jakaa niitä photoshoottien sopimisen yhteydessä cossaajien kanssa.

  form.form-horizontal(method='POST')
    .panel.panel-default
      .panel-heading: strong Ilmoituksen sisältö
      .panel-body
        if can_edit
          - crispy photographer_form
        else
          - readonly photographer_form
      .panel-footer.clearfix
        if can_edit
          .btn-group.pull-right
            a.btn.btn-default(href='{% url "shoottikala_event_view" event.slug %}') Hylkää muutokset ja palaa tapahtumasivulle
            button.btn.btn-success(type='submit') Lähetä ilmoittautuminen
          p.text-muted Nämä tiedot näkyvät valokuvaajaa etsiville cossaajille. Voit muokata näitä tietoja myöhemmin.
        if can_initiate_conversation
          .btn-group.pull-right
            .btn.btn-default(href='{% url "shoottikala_event_view" event.slug %}') Palaa tapahtumasivulle
            a.btn.btn-default(href='{% url "shoottikala_conversation_view" event.slug photographer.id cosplayer.id %}') Lähetä viesti
          p.text-muted Tutustu valokuvaajan esittäytymistekstiin sekä galleria- ja somelinkkeihin. Jos valokuvaaja vaikuttaa sopivalta, ehdota shoottia valitsemalla <em>Lähetä&nbsp;viesti</em>.
